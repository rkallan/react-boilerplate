const getThemeState = ({ theme }) => theme;
const getThemeTemplate = ({ theme }) => theme.template;
const getThemeColorScheme = ({ theme }) => theme.colorScheme;

export { getThemeState, getThemeTemplate, getThemeColorScheme };
